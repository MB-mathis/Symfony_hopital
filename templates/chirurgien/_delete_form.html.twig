<form method="post" action="{{ path('app_chirurgien_delete', {'id': chirurgien.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chirurgien.id) }}">
    <button class="btn">Delete</button>
</form>
