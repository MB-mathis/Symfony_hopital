<form method="post" action="{{ path('app_donneur_delete', {'id': donneur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donneur.id) }}">
    <button class="btn">Delete</button>
</form>
