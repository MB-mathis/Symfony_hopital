{% extends 'base.html.twig' %}

{% block title %}DossierMedical{% endblock %}

{% block body %}
    <h1>DossierMedical</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dossier_medical.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ dossier_medical.createdAt ? dossier_medical.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ dossier_medical.updatedAt ? dossier_medical.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>NumeroDossier</th>
                <td>{{ dossier_medical.numeroDossier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path(constant('App\\Controller\\DossierMedicalController::ROUTE_LIST')) }}">back to list</a>

    <a href="{{ path(constant('App\\Controller\\DossierMedicalController::ROUTE_UPDATE'), {'id': dossier_medical.id}) }}">edit</a>

    {{ include('dossier_medical/_delete_form.html.twig') }}
{% endblock %}
